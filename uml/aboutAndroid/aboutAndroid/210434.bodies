class InstalledAppDetailsTop
!!!491138.java!!!	getIntent() : Intent
        Intent modIntent = new Intent(super.getIntent());
        modIntent.putExtra(EXTRA_SHOW_FRAGMENT, InstalledAppDetails.class.getName());
        return modIntent;
!!!491266.java!!!	isValidFragment(in fragmentName : String) : boolean
        if (InstalledAppDetails.class.getName().equals(fragmentName)) return true;
        return false;
